# Дело

Актуализация 11 сентября, production sleep fix: первый реальный сон воспроизвёл исчезновение виджета как native crash `0xc0000005` в `GlassRenderer::Tick` (RVA `0x21BCE`). Причина — вложенное power/display message освобождало GPU через `Reset()` во время текущего WGC/DComp Tick. Reset теперь откладывается до выхода из Tick. Повторный реальный сон с работающим таймером PASS: suspend/resume получены, окно видимо, renderer healthy/errors=0, recoveries 1→4, задача paused и сон исключён из elapsed; новых WER/Application Error нет. После relaunch ещё 4 PASS, состояние сохранено. В EXE добавлен VERSIONINFO 0.1.1.0/0.1.1. Финальный installer пересобран, установлен, запущен и удалён; SHA-256 `2D4CDCDEDE24AC6142190174E2A7BEF08FBC39102A1BD61A228C7A3D3B6BDC8A`.

Актуализация 11 сентября, functional/lifecycle: live WebView2/host проверки закрыли меню (14), quick-window (9), ввод/редактор (9), RU/UK/EN (13), undo/корзину (12) и recovery повреждённого `tasks.json` из backup (4). Исправлены live overdue sound, очистка quick-черновика по Escape и пауза undo при фактическом фокусе. 28 JS, 20 store/hotkey и 9 GPU проверок PASS. На неподвижном desktop production renderer за 10 секунд выполнил 0 новых presents, остался healthy/errors=0; процесс использовал около 0,13% одного ядра и 54,69 МиБ Working Set. Цикл functional-пакета с relaunch и сохранением данных также PASS; его контрольная сумма историческая, актуальная после sleep-fix приведена выше. Физические multi-monitor/DPI и чистая Windows остаются UNVERIFIED.

Актуализация 11 сентября, W01: production `app/bin/Delo.exe` прошёл настоящий Win+D в отдельной harness-сессии. До и после: фактические `IsWindowVisible=true`, `IsIconic=false`, owner/window не изменились, renderer healthy и без ошибок, процесс отвечал. Реальный сон app и multi-monitor/DPI остаются UNVERIFIED. Добавленные diagnostics уже вошли в пересобранный пакет из checkpoint выше. Подробнее TASKS.md и architecture/APP-LIFECYCLE.md.

Актуализация 11 сентября, H06: локальный installer 0.1.1 собран, выполнены установка 0.1.0 в отдельный каталог, обновление поверх неё, запуск установленной копии и удаление. Задача пережила обновление и удаление с неизменным SHA-256; EXE/ярлык/uninstall key удалены, общий WebView2 сохранён. Проверка была на машине с уже установленным runtime, поэтому ветка его установки и чистая Windows без SDK ещё не доказаны. Подробности в TASKS.md и architecture/DISTRIBUTION.md. Исторические статусы ниже не заменяют этот checkpoint.

Текущий этап — Implementation/Verification по harness в TASKS.md. Рабочая app/ собирается и проверена в настоящем Win32+D3D11+WebView2 host; installer существует, но это ещё не публичный релиз. П1–П8 используются как явно записанные implementation defaults. Следовать актуальному TASKS.md, не выдавать частично закрытый H06 за полный V12.

Ограничение частоты исследовано и исправлено в StabilityProbe: architecture/FRAME-PACING.md. Источник теперь 30 обновлений/с, итоговый профиль 29,25 отрисовок/с, в покое 0. Точные часы и локальные waitable timers заменили грубое планирование и Sleep(1) в детекторе. Полный автоматический прогон и профиль прошли. Два реальных сна и пятиминутный Soak ниже относятся к прежней сборке; после этой правки не повторены.

Пятиминутный Soak завершён: architecture/STABILITY.md, 65 проверок, ошибок 0. Память стабильна, в покое нет отрисовок; преломление и вывод сохранены. При движении около 20 отрисовок/с для источника с целевыми 30 обновлениями/с — требуется профилирование конвейера. Многочасовой тест не выполнен. Запуск: prototypes/gpu-glass/run-stability.ps1 -Soak.

Устойчивость GPU-стенда: architecture/STABILITY.md — автоматический повтор 173 успешные проверки, два отдельных реальных сна по 17 успешных проверок, ошибок 0. После пробуждения восстановлены захват, преломление, самоисключение и фактический вывод. Физическая смена мониторов/DPI, настоящий сбой драйвера, перезапуск Explorer и длительная нагрузка ещё не проверены. Это стенд, не готовый планировщик.

Процесс: Наброски → SPEC → Architecture → Roadmap → Tasks → Implementation → Verification. Текущий этап — Architecture: читать SPEC.md и ARCHITECTURE.md v0.1; оставшиеся предложения П1–П8 ещё не утверждены пользователем. Не выдавать интерактивный набросок за готовое Windows-приложение.

Начата техническая проверка для Architecture: architecture/WINDOWS-LAYER.md. Правило повторного автоархива после восстановления подтверждено в SPEC A04 и заменяет раннее предложение о 7 днях защиты. Windows-прототип не является выбором стека и не проверяет Liquid Glass.

Первая проверка Composition-материала не прошла: architecture/NATIVE-MATERIAL.md. Её нельзя выдавать за успешную интеграцию стекла или доказательство невозможности эффекта на Windows. В будущем прототипе нужно проверить native host без WinForms и реальный источник фона.

Native host уже создан: architecture/NATIVE-WINDOW.md. Повторный прогон с Saturation — 20/25: обработка внешнего фона обычным BackdropBrush подтверждена во всех трёх конфигурациях, включая desktop после Win+D. HostBackdrop не работает; пять ошибок сохранены в журнале. Блокировка по кредитам устранена, текущая сборка запущена. Этот успех относится к обработке цвета; результат преломления приведён ниже.

Преломление проверено отдельно и не прошло: architecture/REFRACTION.md. Прямой affine+BackdropBrush даёт E_INVALIDARG; через VisualSurface внешняя сетка не смещается, хотя внутренняя контрольная сетка изгибается (1173 пикселя). Ввод/переключения с оптикой подготовлены, но не выполнялись. Следующий эксперимент — другой механизм, WinUI 3 / HLSL-кандидат; стандартный путь не повторять без новой гипотезы. Производственный стек не выбран.

При изменениях интерфейса прочитать и применять `skills/apple-liquid-glass/SKILL.md`. Это проектный источник правил Apple Liquid Glass, созданный по запросу пользователя. Сохранять рабочее преломление, обе темы и единые элементы управления. Использовать доступный ui-ux-pro-max для UX-проверки. Новые указания пользователя имеют приоритет над сохранёнными примерами.

Актуальный макет: `design/index.html`. Старый `sketches.html` отклонён. Исследование и ограничения: `design/REFERENCES.md`; сценарии: `SKETCHES.md`.

WinUI 3 / HLSL-кандидат теперь собран и проверен: architecture/WINUI-GLASS.md. LiquidGlassWinUI 1.0.3 + App SDK 2.2.0 не показал преломления в четырёх независимых сценариях. Внешний фон обновляется без смещения, внутреннее изображение перестаёт видимо обновляться после подключения эффекта. LastError пуст — это не критерий успеха. Пробник: prototypes/winui-glass. Ввод и desktop-переходы с этой оптикой не проверены; стек не утверждён.

Актуальный успех: отдельный D3D11/HLSL-пробник prototypes/gpu-glass — 39/39 проверок, architecture/GPU-GLASS.md. Преломление GPU-текстуры и живого отдельного HWND, обе темы, реальный вывод и отсутствие захвата перекрывающего окна подтверждены. Следующее — реальный фон за виджетом (monitor capture и исключение виджета), затем desktop/topmost/Win+D и ввод с новым renderer. Не путать результат собственного HWND с готовым захватом рабочего стола.

Реальный фон монитора теперь проверен: prototypes/gpu-glass/MonitorProbe.cpp и architecture/MONITOR-GLASS.md — 25/25, исключение собственного top-level HWND подтверждено контрольной пурпурной панелью и повторными обновлениями. Снимки пользовательского рабочего стола остаются в игнорируемом bin, в evidence только журнал и искусственная сетка. Следующий этап — desktop/topmost/Win+D/ввод с renderer; проверить исключение при смене оконной модели (SetWindowDisplayAffinity документирован для top-level HWND). Окна пользователя намеренно оставлены свёрнутыми.

Оконные режимы с материалом проверены: architecture/GLASS-WINDOW-MODES.md — 49 проверок плюс 4 диагностические записи, ошибок 0. Рабочая модель: top-level WS_POPUP с владельцем desktop-host Explorer через GWLP_HWNDPARENT; при topmost владелец снимается. Виджет ниже обычных окон на desktop, переживает Win+D, сохраняет преломление/самоисключение/ввод. Прямой WS_CHILD даёт ошибку 87 при WDA и отвергнут. Чернота ранних кадров была недостоверным контролем GDI-источника; исправлены layered desktop-source и manifest Windows 10. Следующие проверки: восстановление owner/capture/device, сон/DPI/размеры/мониторы, нагрузка и idle. Explorer пользователя не перезапускать без отдельной договорённости.
