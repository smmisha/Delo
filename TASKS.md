# Delo — harness реализации

Цель: законченный виджет по SPEC, автономная поставка Inno Setup и публичный исходный код/релиз. Ни один успешный графический стенд не закрывает продукт целиком.

## Решения реализации

Новая команда пользователя — довести сам виджет до конца — переводит работу в реализацию. П1–П8 SPEC используются как явно записанные рабочие defaults, а не как цитата одобрения каждого пункта пользователем. Они сохраняют требования и могут корректироваться. Лимит текста — 4000 символов; сохраняемый checkpoint таймера — каждые 5 секунд. Лицензия приложения планируется MIT, проверяется состав ресурсов перед публикацией.

Стек: Win32 C++17, D3D11/WGC/DirectComposition из проверенного собственного renderer, прозрачный WebView2 с локальными HTML/CSS/ES modules. WebView2 SDK 1.0.4191.47, установленный Evergreen runtime. Production-интеграция WebView2 со стеклом должна пройти отдельную проверку; её успех пока не предполагается. Нативный процесс один; два окна используют одно хранилище с ревизиями. Локальный JSON envelope сохраняется атомарно с резервной копией, в профиле пользователя. JS domain отделён от UI и файловой системы.

## Этапы и доказательства

| Этап | Требования | Результат / критерий | Статус |
| --- | --- | --- | --- |
| H01 Domain | T01–T06, F01–F02, R01–R03, A01–A04, D01–D03 | Чистая модель и тесты V05–V09, границы времени, idempotency, сохранение схемы | Выполнен |
| H02 Storage | N01–N02, V11 | Атомарная запись, backup, конфликт ревизий, повреждение, восстановление, checkpoint | Выполнен |
| H03 Native shell | W01–W05, N03 | Постоянное окно, desktop/topmost, hotkeys, tray, singleton, DPI, сон/Explorer | В работе |
| H04 UI | S04, T/U, V03–V04,V10 | Принятый дизайн, обе темы, 3 языка, клавиатура, формы/архив/корзина, error/empty | Выполнен |
| H05 Integration | U01–U02,V01–V11 | Настоящий desktop background и резкий UI; сохранённая задача после relaunch; регрессии | В работе |
| H06 Distribution | N04,V12 | Inno Setup, runtime, установка/обновление/удаление с сохранением данных | В работе |
| H07 Release | N05 | Публичный репозиторий, лицензии, README, installer release, secret/artifact audit | В работе |

## Последний checkpoint реализации

11 сентября, V01/V10/publication prep: live reduced motion — 4/4 PASS, настройка применяется и сохраняется, button transition и row displacement animation отсутствуют. Физический Win32 Ctrl+Alt+N открыл quick-window с фокусом и сохранённым previous HWND; Ctrl+Alt+Space скрыл quick и открыл main list. Созданы root README, THIRD_PARTY, `.gitignore`, `.gitattributes` и локальный Git repository. Публикационный allowlist содержит 197 файлов общим размером 1,36 МиБ; bin/obj/vendor/dist/test-output исключены, secret patterns и абсолютные пути пользователя не найдены. Матрица всех требований — `architecture/REQUIREMENTS-AUDIT.md`.

11 сентября, production sleep fix: первый реальный сон с работающим таймером воспроизвёл исчезновение виджета как `APPCRASH 0xc0000005`, RVA `0x21BCE` внутри `GlassRenderer::Tick`. WGC/DComp мог вложенно доставить power/display message, а прежний `Reset()` освобождал GPU во время разыменования. `Rebuild` и `Suspend` теперь откладывают reset до выхода из Tick и защищают его от повторного входа. Сборка, 28 JS и 9 GPU проверок PASS. Повторный реальный сон: 6/6 PASS — suspend/resume получены, process/window живы и видимы, renderer healthy/errors=0, recoveries 1→4, running task стала paused, сон исключён из elapsed. Новых WER/Application Error нет. После штатного выхода и relaunch той же сессии ещё 4/4 PASS: задача, paused и elapsed сохранены, renderer healthy. В EXE добавлен VERSIONINFO 0.1.1.0/0.1. Финальный пакет пересобран, установлен в новый каталог, запущен с healthy renderer и удалён без остатка приложения; SHA-256 `2D4CDCDEDE24AC6142190174E2A7BEF08FBC39102A1BD61A228C7A3D3B6BDC8A`. Подробности — `architecture/APP-LIFECYCLE.md` и `architecture/DISTRIBUTION.md`.

11 сентября, functional/lifecycle verification: в настоящем Win32/WebView2 host проверены меню — 14 PASS, quick-window — 9 PASS, ввод/редактор — 9 PASS, RU/UK/EN — 13 PASS, undo/корзина — 12 PASS и восстановление намеренно повреждённого изолированного `tasks.json` из backup — 4 PASS. Исправлены три первопричины: overdue sound теперь срабатывает ровно на новом live tick-событии, Escape в quick очищает отменённый черновик, undo ставится на паузу по фактическому фокусу внутри панели. Повторно PASS: 28 JS, 20 store/hotkey и 9 GPU. На неподвижном desktop production renderer за 10 секунд получил 4 capture frames и выполнил 0 новых presents, остался healthy/errors=0; за 12 секунд процесс использовал 15,62 мс CPU, Working Set стабилен на 54,69 МиБ. Подробности — `architecture/APP-LIFECYCLE.md`.

11 сентября, актуальная поставка: после functional исправлений `Delo-0.1.1-windows-x64-setup.exe` был установлен в новый отдельный каталог; 18 файлов соответствовали allowlist, исходников/тестов/vendor не было, установленный EXE совпал с release SHA-256. Установленная копия загрузила настоящий host с renderer healthy/errors=0, сохранила задачу и показала её после relaunch. Деинсталляция завершилась кодом 0: EXE, uninstaller, ярлык и uninstall key удалены, WebView2 сохранился, изолированный `tasks.json` остался с неизменным SHA-256. После sleep-fix финальная сборка и короткий install/launch/uninstall цикл повторены; актуальный hash приведён выше. H06 остаётся «В работе» только из-за непроверенной ветки отсутствующего WebView2 и чистой Windows без SDK.

11 сентября, W01 production Win+D: в harness diagnostics добавлены фактические `IsWindowVisible`, `IsIconic` и owner HWND. Для `app/bin/Delo.exe --harness=lifecycle-20260911` до и после настоящего Win+D: `windowVisible=true`, `iconic=false`, owner/window без изменений, renderer `healthy=true`, `errors=0`; copied вырос с 76 до 128, процесс отвечал. Повторное Win+D вернуло окна, приложение штатно завершилось. Этот сценарий на текущем основном мониторе PASS. Реальный сон именно app, Explorer restart и физические multi-monitor/DPI остаются UNVERIFIED. Подробности — `architecture/APP-LIFECYCLE.md`. Диагностическое изменение уже вошло в актуальный пересобранный пакет.

11 сентября, H06 Distribution: добавлены автоматическое обнаружение MSVC/Windows SDK, MIT `LICENSE`, Inno Setup на RU/UK/EN, подписанный Microsoft standalone WebView2 x64, скрипты подготовки поставки/упаковки и SHA-256. Принятый знак портфеля с галочкой встроен как multi-size icon в EXE, окна, трей и setup; Windows-декодер и извлечение из EXE подтверждены. В отдельном каталоге выполнены установка 0.1.0, обновление до 0.1.1, запуск установленной копии и удаление. После обновления EXE совпал со свежей на тот момент сборкой, UI загрузил прежнюю задачу без ошибки. После удаления исчезли файлы приложения, ярлык и uninstall key; общий WebView2 остался. Изолированный `tasks.json` сохранил SHA-256 `8F4DF9EC8263968944C2C6DED33D5831CB1A51BE415F71A9ED38A0C5C3763540`. Отдельная mutex-проверка подтвердила отказ setup при работающем Delo без изменения файлов. Актуальный пакет и его свежий цикл приведены выше. Ветка установки отсутствующего runtime и чистая Windows без SDK остаются UNVERIFIED; H06 и V12 не закрыты. Подробности — `architecture/DISTRIBUTION.md`.

11 сентября, W03/W04: повторный Quick больше не перезаписывает исходное окно возврата фокуса. `ui-quick.js` в настоящем WebView/host — 8 PASS: фокус ввода, сохранение черновика и предыдущего HWND при повторе, Enter/ровно одна задача/возврат фокуса, Escape без сохранения/возврат фокуса. Вызовы идут через native bridge; физические сочетания клавиш из стороннего приложения остаются отдельной проверкой. Конфликт hotkey теперь явно локализован RU/UK/EN, включая startup. `test-hotkey-conflict.ps1` действительно удерживает Ctrl+Alt+Shift+Z в другом процессе: ошибка видима, старые сочетания сохранены. `test-startup-conflict.ps1` удерживает Ctrl+Alt+Space перед запуском отдельной сессии: ошибка старта видима, настройки доступны; после освобождения сочетания сохранение проходит и предупреждение исчезает. Исправлено устаревшее hotkeyError при обновлении полного native state и успешном сохранении неизменённых настроек. Повтор startup-теста PASS. Первый прогон обнаружил гонку обнаружения CDP-страницы; ожидание целевой страницы/готовности теперь ограничено 5 секундами. 24 JS-теста повторно PASS; сборка без предупреждений. Реальные сон/Win+D, runtime поставки, установщик и релиз не закрыты.

11 сентября, native material review: получены локальные снимки настоящего HWND в обеих темах (не только прозрачный WebView). Обнаружены повторные контуры фона от 17 редких выборок старого шейдера. Заменены двумя Gaussian-проходами по 25 выборок; отдельный optical pass сохраняет inward refraction и rim. Светлый tint повышен с 0.43 до 0.72 для тёмного рабочего стола. Сборка прошла без предупреждений. `app/test-renderer.ps1` — 9 PASS на реальном GPU с искусственным ramp/impulse: обе темы, прозрачные углы, tint, смещение границы, отсутствие вторичных контуров. `app/test-store.ps1` — 20 PASS. Живые dialog checks повторены — 8 PASS, renderer healthy=true/errors=0. Снимки остаются в игнорируемой сессии и не предназначены для публикации. Новая GPU-нагрузка/длительный idle и реальный сон приложения ещё не проверены.

11 сентября, сессия `--harness=review-20260911`: запуск разрешён и выполнен, прежняя блокировка GUI снята. Исправлена регрессия закрытых диалогов: `display:flex` применялся к editor/settings без `[open]`. Живые проверки `app/tests/ui-dialogs.js` — 8/8 (скрытие, открытие, видимая кнопка сохранения, закрытие). 24 JS-теста прошли. Renderer diagnostics: healthy=true, errors=0, copied=413, rendered=49; это не заменяет визуальное подтверждение оптики. Двойная отправка создала одну задачу; таймер сохранил 1264,73 ms, выполнение начислило +5. После штатного выхода процесс отсутствовал; повторный запуск той же сессии сохранил задачу, время, статус выполнения и +5 без ошибки UI. Личные данные прежнего harness не изменялись. Git diff пока недоступен: Delo ещё не Git-репозиторий. Реальный сон производственного приложения, обе темы и native screenshot остаются непроверенными.

Дополнение: восстановление backup добавлено в UI на RU/UK/EN с объяснением и подтверждением. Схема проверяется до загрузки списка; повреждение не подменяется пустыми данными. Исправлены reducedMotion и общий предел 4000 символов. Выход отменяется при отсутствии подтверждения финального сохранения. Restore повышает revision и рассылает новое состояние обоим окнам. Повторная сборка без предупреждений; 20 JS-тестов и 13 store checks прошли. Визуальная проверка этих новых действий ещё не выполнялась. Предыдущий goal-turn классифицирован как progress: изменены код, тесты и получены новые проверяемые результаты. Цель активна.

10 сентября: добавлены app/native (host, renderer, atomic store), app/core и app/ui. app/build.ps1 собирает Delo.exe без предупреждений. `node --test app/tests/*.test.mjs` — 15/15; `app/test-store.ps1` — 9/9. Проверена контрольная сумма WebView2 SDK. Эти проверки доказывают части H01/H02, а не полный продукт. Нативный GUI не запускался: автоматический approval-review отклонил запуск из-за исчерпания кредитов workspace, обход не выполнялся. Следующий обязательный шаг H05 — запуск в --harness и проверка UI/стекла/сохранения. До запуска можно продолжать код-ревью и тесты.

Незакрытые известные части: end-to-end UI восстановления backup и lifecycle checkpoint; выравнивание стекла/HTML; проверка фокуса, языков и всех форм; локализация трея и иконка; проверка/доработка поставки runtime и Inno Setup; публичный репозиторий/релиз. Полный requirement audit ещё не выполнен.

## Правила harness

Каждый этап оставляет реальные журналы и повторяемую команду. PASS означает проверенный сценарий; отсутствие среды или внешнего монитора означает UNVERIFIED, не PASS. Последний completion audit сопоставляет каждый ID SPEC и V01–V12 с кодом и свидетельствами. Не маркировать цель завершённой, пока остаётся обязательный непроверенный результат. Реальные личные данные и скриншоты рабочего стола не попадают в публикуемые evidence.

Исходные графические проверки: architecture/STABILITY.md и FRAME-PACING.md. Они относятся к стендам и не подменяют H05.
