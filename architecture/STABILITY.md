# Устойчивость GPU-пробника — 10 сентября 2026

Обновление: причина ограничения частоты найдена и исправлена, новый профиль — 29,25 кадра/с. [Результаты и границы повторной проверки](FRAME-PACING.md). Реальные сны и Soak этого отчёта относятся к предыдущей сборке.

Реальный сон: `stability-23820203`, 17 успешных проверок, ошибок 0. Получены suspend/resume, после разблокировки восстановлены захват, преломление, исключение собственного окна и фактический вывод.

Повтор реального сна: `stability-37534296`, ещё 17 успешных проверок, ошибок 0. Получены suspend=1, resume=1, display=2 и события блокировки/разблокировки. Все проверки после сна прошли; процесс завершился. Журнал: `evidence/stability-2026-09-10/real-sleep-repeat.jsonl`. Итого подтверждены два отдельных прогона реального сна.

Повтор автоматического стенда: `stability-37407890`, exit 0, 173 успешные проверки. Проверены закрытие захвата, внедрённые ошибки устройства и доступа, ограниченные повторы, синтетические уведомления сна/дисплея/DPI, реальные изменения размеров ресурсов, закрытие источника, потеря суррогатного владельца, возврат из-за границ экрана и восемь циклов восстановления. Реальный Explorer не перезапускался. Один физический монитор, системный DPI 120, AMD Radeon 610M.

Короткие замеры: idle 12 секунд — CPU 0,012% всей машины, working set 54,7 МиБ, 0 отрисовок, 71 копирование кадров. Движущийся источник 12 секунд — CPU 0,11%, 233 отрисовки; это не подтверждение стабильных 30 FPS. После движения снова 0 отрисовок. Освобождённое состояние — 0 копирований и отрисовок. Медиана GPU-времени шейдера 132,76 мкс, p95 136,2 мкс; это не полная задержка захвата/вывода и не замер энергопотребления.

Начальный повтор в песочнице `stability-37398375` завершился исключением 2147943460. Успешный прогон выполнен вне песочницы на обычном рабочем столе. Ошибка первого запуска не включена в успешный результат.

Хеши исходников и EXE совпали с сохранённым build.json. Журналы находятся в `evidence/stability-2026-09-10`; снимки рабочего стола туда не копировались. Git-проверка недоступна: Delo не является Git-репозиторием.

## Пятиминутный прогон

`stability-38111546`, режим `-Soak`: 65 успешных проверок, ошибок 0, exit 0. Десять замеров по 30 секунд в одном процессе, чередование движения и покоя, дополнительный прогрев. После прогона повторно проверены источник, преломление, центр, самоисключение и видимый вывод. Неожиданных пересозданий графики не было.

Working set составил от 49,4 до 50,7 МиБ, private memory от 31,57 до 31,73 МиБ. Накопительного роста за этот период не наблюдалось. Число дескрипторов оставалось в заданном допуске относительно прогрева. Во всех пяти интервалах покоя было 0 отрисовок. CPU всей машины при движении от 0,112% до 0,177%, в покое около 0,02% до 0,03%.

За 30 секунд движения выполнено от 587 до 612 отрисовок: около 20 в секунду при источнике с целевыми 30 обновлениями в секунду. Это ограничение текущего стенда, стабильные 30 FPS не подтверждены. GPU-время одного шейдера: медиана 145,56 мкс, p95 153,32 мкс; оно не объясняет полную задержку конвейера. Следующая локальная техническая работа — измерить ожидания захвата/проверки изменений/вывода и устранить ограничение частоты, сохранив отсутствие отрисовок в покое.

Добавлены только режим Soak и его запуск; существующие сценарии сна и автоматического восстановления не изменены. MSVC-сборка и синтаксис PowerShell прошли; одновременные `-ManualSleep -Soak` корректно отклоняются до запуска процесса. Журнал `evidence/stability-2026-09-10/soak.jsonl`, хеши этой сборки `soak-build.json`. Старый build.json относится к прежним прогонам.

Остаются непроверенными физическая смена мониторов/DPI, настоящий сбой драйвера, перезапуск Explorer и многочасовая нагрузка. Пять минут не исключают медленных утечек. Это графический стенд, а не готовый планировщик.
