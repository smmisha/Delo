# Проверка визуального наброска v2

## Дополнение: набросок v3, 9 сентября 2026

- 5 автоматических тестов task-model.test.mjs прошли: накопление времени, пауза/стоп/продолжение, единственный работающий таймер, работа без таймера, обратимость награды и календарные даты.
- node --check для preview.js и tasks.mjs проходит.
- В браузере проверены: запуск → пауза (жёлтая) → стоп (нейтральный, 9 секунд сохранены), редактирование названия, срок «Завтра», произвольная дата 1 декабря, снятие срока, удаление и отмена с сохранением названия, даты и времени.
- Просмотрены обе темы, жёлтая работа без таймера с отдельным красным просроченным сроком, стеклянный редактор и меню.
- Проверены ширины 887 и 375 px: горизонтального переполнения страницы и строк нет. Меню помещается в узком viewport. Размер viewport восстановлен.
- Escape закрывает меню/настройки. Переключатель уменьшенного движения устанавливает соответствующее состояние. Постоянной подписи desktop-режима в DOM больше нет.
- Ошибок и предупреждений приложения в журнале браузера при проверках не обнаружено.
- Новый apple-liquid-glass прошёл штатный quick_validate.py в проекте и после установки в ~/.codex/skills. Для валидатора использован уже имеющийся PyYAML из локального окружения watermark_remover; новые пакеты не устанавливались.
- Старый пользовательский макет с его пробными задачами не перезагружался; новая версия открыта отдельной вкладкой.

Ограничения v3: задачи в памяти вкладки, баллы демонстрационные, выбранные даты отображаются и группируют задачи, но полный движок просрочек/штрафов/архива ещё не реализован. Это интерактивный набросок перед SPEC.

## История проверки v2

9 сентября 2026. Это проверка макета, не финальный этап Verification приложения.

## Выполнено

- Официальная страница Apple открыта в Chrome, визуально просмотрены демонстрации Liquid Glass и Apple Music.
- В Chrome открыт локальный design/index.html; основной WebGL-шейдер компилируется и отображает материал.
- Визуально просмотрены светлая и тёмная темы, основной виджет и стеклянная капсула быстрого ввода.
- Переключение фона и темы работают.
- Ввод длинной задачи через капсулу и Enter добавляет её в список; фокус возвращается на кнопку вызова.
- Выполнение меняет группу, зелёное состояние и пример репутации. Отмена возвращает 125 баллов после демонстрационного перехода 125 → 130 → 125.
- Таймер запущен: наблюдался переход 00:00:00 → 00:00:03.
- Панель настроек открывается, доступны переключатели звука и движения.
- При viewport 375×812 у строк scrollWidth равен clientWidth; горизонтального переполнения страницы не обнаружено. Сделан визуальный просмотр. Размер viewport восстановлен.
- node --check design/preview.js проходит.
- После заключительных правок повторно просмотрен журнал Chrome: ошибок и предупреждений приложения нет.

## Ограничения

- Нативный backdrop Windows, Win+D, глобальные сочетания, локальное сохранение и установщик не реализованы на стадии набросков.
- Основная панель и капсула преломляют демонстрационный фон, не произвольное содержимое других окон. Настройки и небольшие кнопки используют CSS-поверхности.
- Цвета подписей затемнены после визуального просмотра. Численный замер контраста по пикселям через API браузера недоступен (DOM-прокси не предоставляет canvas.getContext); полное соответствие WCAG не заявляется.
- Звуковой сигнал реализован, факт его слышимости не проверен.
- В этом эскизе только русский текст. Локализации и системный режим reduced-motion ещё требуют проверки на этапе реализации.
- Git diff пока недоступен: папка Delo и её родитель не являются Git-репозиторием. Проверялся список созданных файлов; зависимости и секреты не добавлялись.

# Повторная проверка после обновлений Claude — 10 сентября 2026

Актуальный `index.html` (набросок 4) проверен в отдельной вкладке встроенного браузера, без изменения пользовательских демонстрационных задач. Дизайн и логику Claude при этом не меняли.

- Просмотрены светлая и тёмная темы, новый векторный логотип и стеклянный редактор.
- Shift+Enter добавляет перевод строки; Enter сохраняет многострочную задачу, очищает ввод и отключает стрелку добавления.
- Черновик переносится из основного ввода в редактор срока. Выбор «Завтра» отражается в `aria-pressed` и дате; задача появляется в группе «Ближайшие».
- Удаление показывает панель внутри виджета; «Отменить» восстанавливает задачу, включая многострочный текст.
- В консоли проверенной вкладки нет ошибок и предупреждений. При ширине viewport 1280 px горизонтального переполнения страницы нет.
- `node --test design/task-model.test.mjs`: 5/5 успешно. `node --check` для `tasks.mjs` и `preview.js` успешно.
- Пятисекундные окна отмены и их приостановка изучены по коду; полный сценарий истечения нескольких окон, IME и адаптация обновления на узком экране в этой проверке не тестировались.
- В папке Delo нет Git-репозитория, поэтому Git diff недоступен. Публикация навыка ведётся в отдельном репозитории.

Выше сохранены результаты предыдущих версий; они не заменяют проверку новых сценариев.

## Прокрутка меню и переход к SPEC — 10 сентября 2026

- Воспроизведена причина: после PageDown страницы scrollY изменился с 0 до 184, кнопка переместилась с 357 до 173 px, а открытое fixed-меню осталось на 407 px. Прежний обработчик слушал только список.
- В `tasks.mjs` заменён локальный обработчик на capture-listener прокрутки документа. Он закрывает меню при прокрутке внешних контейнеров, оставляет возможную прокрутку самого меню и возвращает фокус на invoker, если фокус был внутри меню.
- Проверены PageDown страницы и внутреннего списка: меню закрыто; у списка scrollTop изменился с 0 до 83 px. Проверены повторное открытие, второй клик, Escape, возврат фокуса и закрытие при изменении размера.
- Просмотрены тёмная тема и светлая при viewport 390 × 844. Меню в границах viewport, горизонтального переполнения нет. Временный размер восстановлен; ошибок и предупреждений в журнале вкладки нет.
- `node --check design/tasks.mjs` и 8 тестов `task-model.test.mjs` / `deletion-window.test.mjs` успешно. Эти тесты проверяют существующий набросок, не требования будущей SPEC.
- Создана SPEC v0.1 с подтверждёнными требованиями и отдельно помеченными предложениями П1–П8. Обновлены статусы AGENTS, SKETCHES и исторического ROADMAP. Нативное приложение не реализовывалось; новых библиотек нет.
- Git diff недоступен: Delo не является Git-репозиторием. Изменение исполняемого кода ограничено обработчиком прокрутки меню.
## Анимации, отсчёт и корзина — 10 сентября 2026

- `node --test design/task-model.test.mjs design/deletion-window.test.mjs`: 8/8 успешно. Новые проверки покрывают границу 5000 мс, синхронность цифры/доли кольца, долгую паузу, независимые окна нескольких удалений и однократную передачу в корзину.
- Проверены удаление, остановка отсчёта при наведении более 5 секунд (цифра и strokeDashoffset неизменны), последовательное удаление двух задач и отмена обоих.
- Проверены истечение окна, переход в корзину, восстановление оттуда и возврат фокуса при закрытии корзины.
- Просмотрены светлая и тёмная темы. При viewport 390 × 844 панель отмены находится выше ввода и не перекрывает его; горизонтального переполнения нет. Временный override размера снят.
- Проверен ручной режим уменьшенного движения: удаление/отмена работают, исчезающая копия строки не создаётся, отсчёт остаётся доступен. Системное переключение настройки реализовано через matchMedia; изменение самой системной настройки в этом прогоне не выполнялось.
- В журнале браузера ошибок и предупреждений нет. Корзина пока существует только в памяти наброска, до перезагрузки страницы.
